{
  "description": "Vigil decision-gate: advisory review before file writes based on trust scores",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pre-tool-use/gate-change.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
